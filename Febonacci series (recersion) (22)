#include <iostream>
using namespace std;

void printFibonacciNums(int num = 10, int pre = 0, int now = 1)
{
	if (num == 0)
		return;
	cout << now << "  ";
	printFibonacciNums(num - 1, now, now + pre);
}

int main()
{
	printFibonacciNums();
	return 0;
}
